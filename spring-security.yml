    server:
      port: 8089

    spring:
      application:
        name: spring-security

    jwt:
      # IMPORTANT: Replace this with a strong, BASE64-encoded secret key.
      # You can generate one using: echo "your-secret-key-here" | base64
      # Or, even better, use an environment variable in docker-compose.yml for production.
      secret: YmFzZTY0ZW5jb2RlZHNlY3JldGtleWZvcmVjb21tZXJjZWFwcGxpY2F0aW9uMjAyNA==
      expiration: 3600000 # 1 hour in milliseconds
      refresh.expiration: 604800000 # 7 days in milliseconds
    
