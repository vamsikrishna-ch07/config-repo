    # Example corrected inventory-service.yml
    server:
      port: 8083

    spring:
      application:
        name: inventory-service

      datasource:
        url: jdbc:mysql://mysql-db:3306/inventory_db?createDatabaseIfNotExist=true
        username: root
        password: Kr12@984872
        driver-class-name: com.mysql.cj.jdbc.Driver

      jpa:
        hibernate:
          ddl-auto: update
        show-sql: true
        # REMOVE THIS SECTION:
        # properties:
        #   hibernate:
        #     dialect: org.hibernate.dialect.MySQL8Dialect
    
