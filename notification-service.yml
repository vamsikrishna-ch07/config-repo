server:
  port: 8088

spring:
  application:
    name: notification-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: vamsikrishnach2000@gmail.com
    password: "bindagiximyqytak"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  cloud:
    loadbalancer:
      retry:
        enabled: true

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

# JWT Configuration for Token Validation
jwt:
  secret: your-super-secret-key-that-is-at-least-256-bits-long-for-hs256
